> 基于契约 PRO-001 v1.0.0 编译 | 语义字典 v1.0.0 | 2026-07-04
> 作用：在 AI 生成过程状态组件前注入语义约束
> 适用范围：conversational 域下的认知阶段展示

---

## 绝对不能碰的红线

1. **禁止用单一"思考中"文案覆盖所有认知阶段**
2. **禁止认知阶段标签与实际行动不符**
3. **禁止同一对话流中阶段标签前后不一致**

---

## 认知阶段定义（语义令牌）

- **research** → `phase.research`（蓝色 + 放大镜图标 + 来源计数）→ 用户行动：查看已找到的来源数量 / 等待检索完成
- **analysis** → `phase.analysis`（黄色 + 大脑图标 + 共识度）→ 用户行动：查看共识度指标 / 查看分歧来源
- **check** → `phase.check`（绿色 + 盾牌图标 + 验证状态）→ 用户行动：查看验证状态 / 查看待核实项
- **output** → `phase.output`（紫色 + 文档图标 + 引用索引）→ 用户行动：查看引用索引 / 复制答案

---

## LLM 强制约束

- 必须显示阶段标签（"检索中"/"综合中"/"验证中"/"生成中"）
- research 必须显示已找到的来源计数
- analysis 必须显示多源共识度
- check 必须标注验证通过/失败的来源
- output 必须附带引用索引，指向检索阶段来源
- 禁止跳过阶段直接显示最终答案

---

*本前缀由编译管线自动生成，禁止手工修改。契约变更后自动重编译。*
